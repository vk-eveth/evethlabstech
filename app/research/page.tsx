import { ArrowUpRight } from 'lucide-react'
import { research } from '@/lib/site-data'
import { PageIntro, Reveal, SiteShell } from '@/components/site-shell'
export default function ResearchPage() { return <SiteShell><main className="content-page"><PageIntro eyebrow="Research & insights" title="Advancing knowledge at the frontier of technology." body="Our research explores the convergence of AI, distributed systems, computational science, and real-world applications." /><div className="research-list">{research.map(([status,title,copy,url,linkLabel],i) => <Reveal key={title}><article className="research-row large"><span className={`status-dot status-${i}`} /><div className="min-w-0"><p className="eyebrow !text-[10px]">{status}</p><h2 className="mt-2 text-2xl font-medium tracking-tight">{title}</h2><p className="mt-3 max-w-2xl leading-7 text-muted-foreground">{copy}</p>{title === 'Earth Observation & Geospatial Analytics' && <p className="mt-3 max-w-2xl text-sm leading-6 text-muted-foreground">This work was not conducted by Eveth Labs Tech. It was a student participation by founder Victor Gervas Mmanda during his studies at SUZA.</p>}{url && <a href={url} target="_blank" rel="noreferrer" className="mt-5 inline-flex items-center gap-2 text-sm font-medium text-primary transition-colors hover:text-accent">{linkLabel}<ArrowUpRight aria-hidden="true" /></a>}</div></article></Reveal>)}</div></main></SiteShell> }
