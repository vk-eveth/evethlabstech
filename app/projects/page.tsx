import Link from 'next/link'
import { ArrowUpRight } from 'lucide-react'
import { projects } from '@/lib/site-data'
import { PageIntro, Reveal, SiteShell } from '@/components/site-shell'
export default function ProjectsPage() { return <SiteShell><main className="content-page"><PageIntro eyebrow="Selected work" title="Building production-ready systems." body="A selection of platforms, infrastructure, research, and prototypes built from first principles." /><div className="grid gap-4 md:grid-cols-2">{projects.map(project => <Reveal key={project.slug}><Link href={`/projects/${project.slug}`} className={`project-card tall accent-${project.accent}`}><div className="flex justify-between"><span className="project-type">{project.type}</span><ArrowUpRight size={17} className="text-muted-foreground" /></div><h2 className="mt-16 text-2xl font-medium tracking-tight">{project.title}</h2><p className="mt-4 max-w-lg leading-7 text-muted-foreground">{project.description}</p><div className="mt-8 flex flex-wrap gap-2">{project.tags.map(tag => <span className="tag" key={tag}>{tag}</span>)}</div><p className="mt-8 text-xs uppercase tracking-[.15em] text-muted-foreground">{project.status} · {project.sdg}</p></Link></Reveal>)}</div></main></SiteShell> }
