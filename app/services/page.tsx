import { services } from '@/lib/site-data'
import { PageIntro, Reveal, SiteShell } from '@/components/site-shell'
export default function ServicesPage() { return <SiteShell><main className="content-page"><PageIntro eyebrow="What we do" title="Engineering solutions across disciplines." body="Our capabilities span the full software engineering lifecycle. We build modular, maintainable systems ready to scale from the start." /><div className="grid gap-4 md:grid-cols-2">{services.map(([num,title,copy]) => <Reveal key={num}><article className="glass-card service-detail"><span className="service-number">{num}</span><h2 className="mt-10 text-2xl font-medium tracking-tight">{title}</h2><p className="mt-4 max-w-md leading-7 text-muted-foreground">{copy}</p></article></Reveal>)}</div></main></SiteShell> }
